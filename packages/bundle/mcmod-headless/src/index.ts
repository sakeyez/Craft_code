/**
 * @deepseek-ai/dsh-mcmod-headless-bundle - headless profile
 * patch layer for the shipped Minecraft mod-development agent. The
 * package's substance is `cordis.patch.yml`, resolved by the profile composer
 * through the `dsh.bundle.patch` manifest field.
 * @module @deepseek-ai/dsh-mcmod-headless-bundle
 */

export {}
