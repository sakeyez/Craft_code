/**
 * @deepseek-ai/dsh-mcmod-bundle — profile patch layer for the
 * shipped Minecraft mod-development Web preset. The package's substance is
 * `cordis.patch.yml`, declared by the `dsh.bundle.patch` manifest field and
 * resolved by the profile composer through that field.
 * @module @deepseek-ai/dsh-mcmod-bundle
 */

export {}
