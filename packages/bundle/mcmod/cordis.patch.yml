- id: agent-presets
  config:
    default: mcmod

- insert:
    - id: lsp
      name: '@deepseek-ai/dsh-lsp'

    - id: lsp-stdio
      name: '@deepseek-ai/dsh-lsp-stdio'
      config:
        servers:
          java:
            command: jdtls
            optional: true
            extensionToLanguage:
              .java: java
