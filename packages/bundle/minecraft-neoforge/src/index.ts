/**
 * @deepseek-ai/dsh-minecraft-neoforge-bundle — profile patch layer for the
 * shipped Minecraft NeoForge Web preset. The package's substance is
 * `cordis.patch.yml`, declared by the `dsh.bundle.patch` manifest field and
 * resolved by the profile composer through that field.
 * @module @deepseek-ai/dsh-minecraft-neoforge-bundle
 */

export {}
