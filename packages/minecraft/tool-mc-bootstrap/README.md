# dsh-tool-mc-bootstrap

Model-facing `bootstrap_mc_project` creates a supported Fabric or NeoForge Java project in an empty directory from a pinned template contract, checks Java availability, and returns phase-structured evidence. It never overwrites a non-empty directory or claims gameplay verification.
